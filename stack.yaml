apiVersion: orun.io/v1
kind: Stack
metadata:
  name: stack-tectonic
  title: Stack Tectonic
  version: 0.14.0
  description: Versioned OCI-hosted catalog of Cloudflare, Terraform and monorepo compositions for Sourceplane repositories.
  owner: sourceplane
  tags:
    - terraform
    - aws
    - cloudflare
    - supabase
    - platform
    - catalog
registry:
  host: ghcr.io
  namespace: sourceplane
  repository: stack-tectonic
  visibility: public
